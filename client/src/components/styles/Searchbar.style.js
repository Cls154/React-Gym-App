import styled from 'styled-components';

export const Div = styled.div`
  display: flex;
  justify-content: center;
  margin-top: 30px;
  height: 45px; 
  margin-bottom: 40px; 
  align-items: flex-end;
`;

export const Input = styled.input`
  height: inherit; 
  width: 800px;
  font-size: inherit;
  border: none;
  border-bottom: 2px solid rgba(31, 32, 36, 0.75);
  outline: none;
  background: transparent;
  text-align: left;
`;

export const Button = styled.button`
 height: inherit; 
 padding: 0 10px;
 border: none;
 border-bottom: 2px solid rgba(31, 32, 36, 0.75);
`;
